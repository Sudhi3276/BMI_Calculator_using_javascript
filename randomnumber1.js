// generation of the random numbers
//it will generate random number b/w 0 to 1
var n=Math.random();
n=n*1000;//convert to corresponding floating point.
n=Math.floor(n);//converting to corresponging integer.
//printing in the console.
console.log(n);